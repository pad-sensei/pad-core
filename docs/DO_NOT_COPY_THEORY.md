# No-copy rule

If a consumer needs a shared musical concept that is not exposed by pad-core, the default action is to extend pad-core's semantic API first.

Copying a theory subgraph/table into a consumer requires an explicit temporary exception plus a parity/removal plan. Presentation-only code is exempt.
